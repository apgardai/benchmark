# CSEA benchmark results

Model run outputs for the CSEA benchmark variant are written here by:

```bash
yarn run:model <target-model> --benchmark csea
```

Layout mirrors `data/model-results/` (one directory per model with `run-meta.json`, `results.json`, `results.zip`, and `testResults/`).
